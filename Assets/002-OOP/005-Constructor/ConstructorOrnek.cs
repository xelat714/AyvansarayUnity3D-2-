using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConstructorOrnek : MonoBehaviour
{
    /*
     Constructor Nedir?(Yapici-Kurucu Metotlar)
     
     
     Ctor, sınıfların tanımlanmasında alt yapıyı oluşturan, kurucu rolü üstlenen, sınıf ismi ile aynı isme sahip olan , geriye değer döndürmeyen fonksiyon türleridir.
    Her class kendi altında default olarak gizli bir ctor oluşturur. Bizim classlarımızın iskelet yapısı ctorlardır.
     
     ctor tab tab ile direk yazılabilir.
     
     
     */







    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
  






}
